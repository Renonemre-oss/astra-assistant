# Core Python Libraries
PyQt6>=6.4.0
PyQt6-WebEngine>=6.4.0

# AI and Machine Learning
scikit-learn>=1.3.0
joblib>=1.3.0
numpy>=1.24.0

# Text-to-Speech
# O motor principal é o Piper (binário externo, ver README). Para a
# funcionalidade opcional de clonagem de voz (XTTS), instala à parte:
#   pip install -r requirements-voice-cloning.txt
# Não está aqui porque o pacote TTS fixa numpy==1.22.0 / tqdm==4.64.*
# em Python <=3.10, o que entra em conflito com o resto do projeto.

# Speech Recognition
SpeechRecognition>=3.10.0
pyaudio>=0.2.11

# Audio Processing
simpleaudio>=1.0.4
pydub>=0.25.1

# Web Search and Requests
duckduckgo-search>=3.9.0
requests>=2.31.0
urllib3>=2.0.0

# Database
# sqlite3 is included with Python standard library
pymysql>=1.1.0  # MySQL remoto (ex.: Railway) via DATABASE_URL

# Image Processing and OCR
Pillow>=10.0.0
pytesseract>=0.3.10
opencv-python>=4.8.0

# Natural Language Processing
nltk>=3.8.0
textblob>=0.17.1

# File and Data Processing
# pathlib - incluído com Python 3.4+
# configparser - incluído com Python standard library
json5>=0.9.14

# Threading and Async
# threading is part of Python standard library

# Utilities
python-dateutil>=2.8.2
tqdm>=4.66.0
tzdata>=2024.1; sys_platform == "win32"  # base de dados de fusos horarios (Windows)

# Development and Testing
pytest>=7.4.0
pytest-cov>=4.1.0
psutil>=5.9.0

# Logging and Monitoring
colorlog>=6.7.0

# GUI and Interface
PyQt6-Qt6>=6.4.0

# Optional: For better Windows compatibility
pywin32>=306; sys_platform == "win32"
# winsound is part of Python standard library

# Hotword Detection / Wake Word
pvporcupine>=2.2.0
vosk>=0.3.45
# vosk-model-small-pt-0.3 should be downloaded separately
